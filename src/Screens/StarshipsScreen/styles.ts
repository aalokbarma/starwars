import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    starshipsScreenMainContainer: {
        flex: 1,
        width: '100%',
        height: '100%',
        padding: 15,
        backgroundColor: "#fbfbfb"
    }
});

export default Styles;