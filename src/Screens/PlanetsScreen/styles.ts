import { StyleSheet } from "react-native";

const Styles = StyleSheet.create({
    planetsScreenMainContainer: {
        flex: 1,
        backgroundColor: '#fbfbfb',
        padding: 15,
    },
});

export default Styles;